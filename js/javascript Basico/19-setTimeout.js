//--------SETTIMEOUT------
/*
El setTimeout nos da la posibilidad de jecutar algo solamente despues de que hayn pasado unos segundos, o minutos.
El setTimeout recibe dos parametros, el primero es una function de callback, y luego recibe otro parametro que es el tiempo en que tarda en ejecutarse ese callback
*/

// setTimeout(() => {
//   console.log("Ejecutado");
// }, 5000);

//Lo mismo con funcions normales

setTimeout(() => {
  console.log("Ejecutado");
}, 5000);
